import classes from "./Header.module.css";
import header from "../assets/header.svg";

function Header() {
    return (
        <div className={classes.header}>placeholder</div>
        // <img src={header} alt="Header" className={classes.header} />
    )
};

export default Header;